defmodule Hello.VenomousPlay do
  # alias Venomous.SnakeArgs
  # import Venomous

  # def case04() do
  #   # pip install -U "transformers>=4.51.0" "sentence-transformers>=2.7.0"
  #   queries = [
  #     "What is the capital of China?",
  #     "Explain gravity"
  #   ]

  #   documents = [
  #     "The capital of China is Beijing.",
  #     "Gravity is a force that attracts two bodies towards each other. It gives weight to physical objects and is responsible for the movement of planets around the sun."
  #   ]

  #   {:ok, result} =
  #     Snake.call(:embedding_worker, :run_embeddings, [queries, documents])

  #   IO.inspect(result, label: "Similarity Matrix")
  # end
end
